import React from 'react';
import styled from 'styled-components';

const BaseLI = styled.li`
  list-style: none;
  white-space: nowrap;

  &:hover {
    cursor: pointer;
  }
`;

export default BaseLI;
